import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");test("customer benefits expose owned BeCoins and notifications",async()=>{const[page,repo,action,migration]=await Promise.all([read("app/minha-area/beneficios/page.tsx"),read("modules/customers/infrastructure/supabase-customer-benefits-repository.ts"),read("app/minha-area/beneficios/actions.ts"),read("supabase/migrations/20260823001500_mark_own_notification_read.sql")]);assert.match(page,/BeCoins e notificações/);assert.match(repo,/becoin_wallets/);assert.match(repo,/notifications/);assert.match(action,/markNotificationReadAction/);assert.match(migration,/user_id=\(select auth.uid\(\)\)/);assert.match(migration,/security definer/)});
