import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");test("customer affiliate and admin share the authenticated visual workspace",async()=>{const[layout,css]=await Promise.all([read("app/layout.tsx"),read("app/internal-unified.css")]);assert.match(layout,/internal-unified.css/);assert.match(css,/\.customer-shell,\.affiliate-shell,\.tool-page/);assert.match(css,/--workspace-violet/);assert.match(css,/\.customer-sidebar/);assert.match(css,/\.affiliate-header,\.tool-header/);assert.match(css,/\.customer-card,\.affiliate-card/);assert.match(css,/@media\(max-width:760px\)/)});
