import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");
test("login redirects every platform role to its dashboard",async()=>{const[destination,action]=await Promise.all([read("lib/auth/dashboard-destination.ts"),read("app/auth/actions.ts")]);assert.match(destination,/super_admin/);assert.match(destination,/\/admin\/suporte/);assert.match(destination,/\/admin\/financeiro/);assert.match(destination,/\/afiliados\/painel/);assert.match(action,/dashboardDestination/);assert.match(action,/safeInternalDestination/)});
test("public storefront exposes a role-aware panel entry",async()=>{const[home,store]=await Promise.all([read("app/page.tsx"),read("app/loja/page.tsx")]);assert.match(home,/Acessar painel/);assert.match(store,/Acessar painel/);assert.match(home,/viewerProfile/);assert.match(store,/viewerProfile/)});
