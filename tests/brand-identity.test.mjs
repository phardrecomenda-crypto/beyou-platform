import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");
test("official visual system centralizes logo, palette, pattern and typography",async()=>{const[logo,css,home]=await Promise.all([read("components/brand-logo.tsx"),read("app/brand-system.css"),read("app/page.tsx")]);assert.match(logo,/beyou-official-logo/);assert.match(logo,/viewBox="0 0 54 32"/);assert.match(css,/--beyou-violet:#908ee9/);assert.match(css,/--beyou-lime:#cfff3d/);assert.match(css,/radial-gradient/);assert.match(css,/--beyou-heading/);assert.match(home,/BrandLogo/)});
