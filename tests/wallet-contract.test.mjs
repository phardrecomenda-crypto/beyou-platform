import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const root=new URL("../",import.meta.url);test("wallet ledger is immutable and payout is atomic",async()=>{const sql=await readFile(new URL("supabase/migrations/20260813180757_affiliate_wallet_foundation.sql",root),"utf8");assert.match(sql,/force row level security/g);assert.match(sql,/idempotency_key text not null unique/);assert.match(sql,/pg_advisory_xact_lock/);assert.match(sql,/INSUFFICIENT_BALANCE/);assert.match(sql,/pix_key_encrypted/);assert.match(sql,/revoke insert,update,delete/)});test("wallet UI has balances, statement and payout",async()=>{const page=await readFile(new URL("app/afiliados/carteira/page.tsx",root),"utf8");assert.match(page,/A liberar/);assert.match(page,/Disponível/);assert.match(page,/Extrato/);assert.match(page,/Solicitar saque/)});
