import Link from"next/link";import{redirect}from"next/navigation";import{createAdminSupabaseClient}from"../../../lib/supabase/admin";import{createServerSupabaseClient}from"../../../lib/supabase/server";import{PLATFORM_ROLES,SupabaseUserAdminRepository}from"../../../modules/admin/infrastructure/supabase-user-admin-repository";import{changeUserRoleAction}from"./actions";import"../../platform-tools.css";
const labels:Record<string,string>={customer:"Cliente",affiliate:"Afiliado",manager:"Gestor",recruiter:"Recrutador",admin:"Administrador",super_admin:"Super administrador",support:"Suporte",finance:"Financeiro"},date=new Intl.DateTimeFormat("pt-BR",{dateStyle:"medium",timeZone:"America/Bahia"});
export default async function UsersAdminPage(){const session=await createServerSupabaseClient(),{data:{user}}=await session.auth.getUser();if(!user)redirect("/login?next=/admin/usuarios");let users;try{users=await new SupabaseUserAdminRepository(createAdminSupabaseClient()).list(user.id)}catch{redirect("/admin")}return <main className="tool-page"><header className="tool-header"><Link className="tool-brand" href="/admin"><b>BE</b>YOU Admin</Link><nav className="tool-nav"><Link href="/admin">Visão geral</Link><Link className="active" href="/admin/usuarios">Usuários</Link><Link href="/admin/produtos">Produtos</Link><Link href="/admin/pedidos">Pedidos</Link><Link href="/admin/suporte">Atendimento</Link></nav></header><div className="tool-main"><section className="tool-title"><small>ACESSO E GOVERNANÇA</small><h1>Usuários e perfis</h1><p>Atribuição centralizada de responsabilidades. Somente o super administrador pode alterar perfis.</p></section><section className="tool-grid"><article className="tool-card wide"><div className="tool-list">{users.map(x=><div className="tool-row" key={x.id}><div><span className="tool-pill">{labels[x.role]??x.role}</span><h3>{x.fullName}</h3><small>{x.city?`${x.city}/${x.state??""} · `:""}Cadastro em {date.format(new Date(x.createdAt))}</small></div><form action={changeUserRoleAction}><input type="hidden" name="userId" value={x.id}/><label>Perfil<select name="role" defaultValue={x.role}>{PLATFORM_ROLES.map(role=><option value={role} key={role}>{labels[role]}</option>)}</select></label><button>Atualizar perfil</button></form></div>)}</div></article></section></div></main>}
