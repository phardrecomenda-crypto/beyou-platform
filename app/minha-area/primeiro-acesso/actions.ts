"use server";import{headers}from"next/headers";import{redirect}from"next/navigation";import{z}from"zod";import{createServerSupabaseClient}from"../../../lib/supabase/server";import{SupabaseCustomerOnboardingRepository}from"../../../modules/customers/infrastructure/supabase-customer-onboarding-repository";
const input=z.object({documentId:z.string().uuid(),accepted:z.literal("on")});
export async function acceptTermsAction(formData:FormData){const parsed=input.safeParse(Object.fromEntries(formData));if(!parsed.success)redirect("/minha-area/primeiro-acesso?erro=aceite");const agent=(await headers()).get("user-agent");try{await new SupabaseCustomerOnboardingRepository(await createServerSupabaseClient()).accept(parsed.data.documentId,agent)}catch{redirect("/minha-area/primeiro-acesso?erro=indisponivel")}redirect("/minha-area/anamnese")}
