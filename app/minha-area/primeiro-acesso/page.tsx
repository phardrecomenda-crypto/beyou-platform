import Link from"next/link";import{redirect}from"next/navigation";import{createServerSupabaseClient}from"../../../lib/supabase/server";import{SupabaseCustomerOnboardingRepository}from"../../../modules/customers/infrastructure/supabase-customer-onboarding-repository";import{acceptTermsAction}from"./actions";import"../customer-area.css";import"../first-access.css";
export default async function FirstAccessPage({searchParams}:{searchParams:Promise<{erro?:string}>}){const query=await searchParams;let data;try{data=await new SupabaseCustomerOnboardingRepository(await createServerSupabaseClient()).load()}catch(error){if(error instanceof Error&&error.message==="AUTHENTICATION_REQUIRED")redirect("/login?next=/minha-area/primeiro-acesso");throw error}if(!data.eligible)redirect("/minha-area");if(data.accepted)redirect("/minha-area/anamnese");if(!data.document)throw new Error("ACTIVE_TERMS_NOT_FOUND");return <main className="first-access"><header><Link href="/minha-area" className="customer-brand"><b>BE</b>YOU<small>Nutrition</small></Link><span>Primeiro acesso · etapa 1 de 2</span></header><section className="first-access-hero"><small>BEM-VINDA À SUA JORNADA</small><h1>Antes de começar,<br/>vamos cuidar do essencial.</h1><p>Leia o termo vigente e confirme o aceite. Depois disso, sua anamnese será liberada.</p></section><section className="terms-card"><div className="terms-heading"><div><small>VERSÃO {data.document.version}</small><h2>{data.document.title}</h2></div><span>Leitura obrigatória</span></div><article>{data.document.body.split("\n\n").map((paragraph,index)=><p key={index}>{paragraph}</p>)}</article><form action={acceptTermsAction}><input type="hidden" name="documentId" value={data.document.id}/><label className="terms-accept"><input type="checkbox" name="accepted" required/><span>Li e aceito esta versão do termo e quero prosseguir para a anamnese.</span></label>{query.erro&&<p className="first-access-error" role="alert">Não foi possível registrar o aceite. Revise a confirmação e tente novamente.</p>}<button>Aceitar e continuar <span>→</span></button></form><p className="terms-note">O aceite registra seu usuário, a versão do documento e a data para auditoria.</p></section></main>}
