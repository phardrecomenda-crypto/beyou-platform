import Link from"next/link";import{redirect}from"next/navigation";import{createServerSupabaseClient}from"../../../lib/supabase/server";import{SupabaseCustomerBenefitsRepository}from"../../../modules/customers/infrastructure/supabase-customer-benefits-repository";import{markNotificationReadAction}from"./actions";const date=new Intl.DateTimeFormat("pt-BR",{dateStyle:"medium",timeZone:"America/Bahia"});
export default async function BenefitsPage(){let data;try{data=await new SupabaseCustomerBenefitsRepository(await createServerSupabaseClient()).load()}catch(error){if(error instanceof Error&&error.message==="AUTHENTICATION_REQUIRED")redirect("/login?next=/minha-area/beneficios");throw error}const unread=data.notifications.filter(x=>!x.readAt).length;return <main className="tool-page"><header className="tool-header"><Link className="tool-brand" href="/"><b>BE</b>YOU Nutrition</Link><nav className="tool-nav"><Link href="/minha-area">Visão geral</Link><Link href="/pedidos">Pedidos</Link><Link className="active" href="/minha-area/beneficios">BeCoins e avisos</Link><Link href="/suporte">Atendimento</Link></nav></header><div className="tool-main"><section className="tool-title"><small>BENEFÍCIOS BEYOU</small><h1>BeCoins e notificações</h1><p>Seu saldo, histórico de movimentações e avisos importantes em uma única área.</p></section><section className="tool-grid"><article className="tool-card third"><small>SALDO ATUAL</small><div className="tool-stat">{data.wallet?.balance??0}</div><p>BeCoins disponíveis</p></article><article className="tool-card third"><small>TOTAL ACUMULADO</small><div className="tool-stat">{data.wallet?.lifetimeEarned??0}</div><p>BeCoins recebidos</p></article><article className="tool-card third"><small>AVISOS NOVOS</small><div className="tool-stat">{unread}</div><p>notificações não lidas</p></article><article className="tool-card"><h2>Extrato de BeCoins</h2><div className="tool-list">{data.entries.map(x=><div className="tool-row" key={x.id}><div><span className="tool-pill">{x.type}</span><h3>{x.description}</h3><small>{date.format(new Date(x.createdAt))}</small></div><strong>{x.amount>0?"+":""}{x.amount}</strong></div>)}{!data.entries.length&&<div className="tool-empty">Nenhuma movimentação registrada.</div>}</div></article><article className="tool-card"><h2>Notificações</h2><div className="tool-list">{data.notifications.map(x=><div className="tool-row" key={x.id}><div><span className={`tool-pill ${x.readAt?"":"alert"}`}>{x.readAt?"LIDA":"NOVA"}</span><h3>{x.title}</h3><p>{x.body}</p><small>{date.format(new Date(x.createdAt))}</small>{x.actionUrl?.startsWith("/")&&<p><Link href={x.actionUrl}>Abrir →</Link></p>}</div>{!x.readAt&&<form action={markNotificationReadAction}><input type="hidden" name="notificationId" value={x.id}/><button>Marcar como lida</button></form>}</div>)}{!data.notifications.length&&<div className="tool-empty">Nenhuma notificação no momento.</div>}</div></article></section></div></main>}
