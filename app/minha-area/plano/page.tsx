import Link from"next/link";import{redirect}from"next/navigation";import{createServerSupabaseClient}from"../../../lib/supabase/server";import{SupabaseAiPlanRepository}from"../../../modules/ai/infrastructure/supabase-ai-plan-repository";import{requestAiPlanAction}from"./actions";import"../../platform-tools.css";
const labels:Record<string,string>={QUEUED:"Na fila",RUNNING:"Em processamento",REVIEW_REQUIRED:"Em revisão profissional",PUBLISHED:"Publicado",REJECTED:"Revisão não aprovada",FAILED:"Processamento indisponível"};const render=(value:unknown)=>typeof value==="string"?value:JSON.stringify(value,null,2);
export default async function PlanPage({searchParams}:{searchParams:Promise<{erro?:string;solicitado?:string}>}){const query=await searchParams;let plan;try{plan=await new SupabaseAiPlanRepository(await createServerSupabaseClient()).load()}catch{redirect("/login?next=/minha-area/plano")}const result=plan?.result;return <main className="tool-page"><header className="tool-header"><Link className="tool-brand" href="/"><b>BE</b>YOU Nutrition</Link><nav className="tool-nav"><Link href="/minha-area">Visão geral</Link><Link href="/minha-area/protocolo">Protocolo</Link><Link className="active" href="/minha-area/plano">Meu plano</Link><Link href="/suporte">Atendimento</Link></nav></header><div className="tool-main"><section className="tool-title"><small>PLANEJAMENTO PERSONALIZADO</small><h1>Meu plano BEYOU</h1><p>Conteúdo educativo criado a partir da sua anamnese e liberado somente após revisão humana.</p></section><section className="tool-grid"><article className="tool-card wide"><h2>{plan?labels[plan.status]??plan.status:"Solicite seu primeiro plano"}</h2>{query.erro&&<p>Para solicitar, conclua uma compra elegível, a anamnese e o consentimento de dados de saúde.</p>}{query.solicitado&&<p>Solicitação registrada. Você poderá acompanhar o andamento nesta página.</p>}{plan?<><span className={`tool-pill ${plan.status==="PUBLISHED"?"good":"alert"}`}>VERSÃO {plan.generationVersion}</span><p>O plano não é diagnóstico, prescrição ou substituto de acompanhamento profissional.</p></>:<form action={requestAiPlanAction}><button>Solicitar meu plano</button></form>}</article>{result&&<><article className="tool-card"><h2>Análise estruturada</h2><div className="tool-plan">{render(result.structured_analysis)}</div></article><article className="tool-card"><h2>Plano semanal</h2><div className="tool-plan">{render(result.weekly_meal_plan)}</div></article><article className="tool-card"><h2>Lista de compras</h2><div className="tool-plan">{render(result.shopping_list)}</div></article><article className="tool-card"><h2>Aviso de segurança</h2><div className="tool-plan">{render(result.safety_notice)}</div></article></>}</section></div></main>}
